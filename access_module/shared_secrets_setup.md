# Portunus Access Module — TLS and Shared Secret Setup

This guide has moved to keep a single source of truth.

The canonical, up-to-date version lives at
[docs/shared_secrets_setup.md](../docs/shared_secrets_setup.md). It covers TLS
setup, the HMAC shared secret, the server-only credential hash secret,
certificate and secret rotation, and the network settings that must match
between the firmware and server.
